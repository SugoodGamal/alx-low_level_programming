#include <stdio.h>

/**
 * main - Entery point
 *
 * Description: print 0, 1, - 9
 *
 * Return: Always 0 (Success)
*/
int main(void)
{
	int gigit = 0;

	while (digit <= 9)
	{
		putchar(digit + 48);

		if (digit != 9)
		{
			putchar(',')
			putchar(' ')
		}
		diit++;
	}
	puchar('\n');
	return (0);
}
